
&НаСервере
Процедура ВыбратьНаСервере()
	
	ПараметрыСеанса.ТекущаяЗонаОтветственности = ЗонаОтветственности;
	
	Закрыть();
	
КонецПроцедуры

&НаКлиенте
Процедура Выбрать(Команда)
	ВыбратьНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(ПараметрыСеанса.ТекущаяЗонаОтветственности) Тогда
		Отказ = Истина;
	КонецЕсли;
	
КонецПроцедуры
